# Terraform State Management - Production Environment
# 
# このファイルは、本番環境のTerraformステートファイルを保存するバックエンドを設定します。
# 
# セットアップ手順:
# 1. このファイルを backend.tf にコピー:
#    cp backend-production.tf.example backend.tf
# 2. YOUR-PROD-PROJECT-ID を実際の本番プロジェクトIDに置き換える
# 3. ステート管理用のCloud Storageバケットを作成:
#    gsutil mb -p YOUR-PROD-PROJECT-ID -l asia-northeast1 gs://YOUR-PROD-PROJECT-ID-terraform-state
# 4. バージョニングを有効化:
#    gsutil versioning set on gs://YOUR-PROD-PROJECT-ID-terraform-state

terraform {
  backend "gcs" {
    bucket = "YOUR-PROD-PROJECT-ID-terraform-state"
    prefix = "terraform/state"
  }
}
